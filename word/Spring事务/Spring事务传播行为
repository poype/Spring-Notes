PROPAGATION_NESTED事务传播行为：
这中事务传播行为实际上是利用了JDBC Connection的setSavepoint方法的能力。
嵌套事务包括父事务和子事务，子事务不能独立提交，当父事务提交时，子事务才能跟着一起被提交；如果父事务回滚，那么子事务也会跟着回滚。
但是子事务自己可以独立回滚。
本质上嵌套事务并不对应两个或多个事务，而是只对应一个事务。
JDBC Connection的setSavepoint方法可以设置一些保存点，当遇到异常时无需rollback整个事务，而是可以返回到之前设置的某个保存点。
对于嵌套事务，其中的子事务实际上就是对应的这些保存点，所以子事务可以独立rollback，但不可以独立commit，只能跟着最外层的整个大事务(父事务)一起commit。


参考文章：https://blog.csdn.net/m0_45406092/article/details/119518594